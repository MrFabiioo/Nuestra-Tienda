---
interface Props {
    src:string;
    alt:string;
    className:string;
}

const  {src,alt,className} = Astro.props;

const currentImage = src.startsWith('http') ? src : `/${src}`;
---

<img src={currentImage} alt={alt}  class={className}>